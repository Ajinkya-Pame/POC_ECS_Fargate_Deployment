# =============================================================================
#  values-qa.yaml — QA environment overrides
# =============================================================================

global:
  environment: qa

frontend:
  replicaCount: 1

backend:
  replicaCount: 1

database:
  replicaCount: 1
  persistence:
    enabled: true
    size: 5Gi

cache:
  replicaCount: 1
