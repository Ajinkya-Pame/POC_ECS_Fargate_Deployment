# Redis configuration — cost-optimized for live scoring cache
# No persistence (ephemeral cache only)

maxmemory 64mb
maxmemory-policy allkeys-lru

# Disable disk persistence (all data is ephemeral cache)
save ""
appendonly no

# Network
bind 0.0.0.0
protected-mode no
port 6379

# Logging
loglevel warning
